extends Node

# vars

# func _ready():
	# pass

# func _physics_process(delta):
	# move_and_slide()

# func walk():
	# walking = true
	# move level towards mech

# func strafe_left():
	# slide level to the right
	# tilt cockpit slightly left for effect?

# func strafe_right():
	# slide level to the left
	# tilt cockpit slightly right for effect?
	
# func attack_left():
	# left weapon animates and attacks (visible from cockpit)

# func attack_right():
	# right weapon animates and attacks (visible from cockpit)

# func take_damage():
	# detect a hit
	# determine how much damage it takes
	# subtract that damage from current health
	# check for lethal
	# if lethal, game over
